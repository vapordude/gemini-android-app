<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark anchors on AI Studio canvas — status/nav bars fade into it. -->
    <style name="Theme.GeminiUI" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/aistudio_bg_dark</item>
        <item name="android:statusBarColor">@color/aistudio_bg_dark</item>
        <item name="android:navigationBarColor">@color/aistudio_bg_dark</item>
    </style>
</resources>
